name: Refresh Mean Citedness analysis (weekly)

on:
  schedule:
    # Monday 05:00 UTC, after the citation refresh
    - cron: '0 5 * * 1'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: pip install -r scripts/requirements.txt

      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.3'
          use-public-rspm: true

      - name: Install R packages
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          packages: |
            any::rmarkdown
            any::jsonlite
            any::ggplot2
            any::scales
            any::patchwork
            any::mgcv
            any::pROC
            any::lme4
          needs: |

      - name: Install Pandoc
        uses: r-lib/actions/setup-pandoc@v2

      - name: Refresh FLoRA snapshot
        run: python scripts/refresh_flora.py

      - name: Enrich with OpenAlex Mean Citedness
        env:
          MY_EMAIL: ${{ secrets.MY_EMAIL }}
        run: python scripts/compute_omc.py

      - name: Render Mean Citedness analysis
        run: Rscript scripts/render_impact_factor.R

      - name: Commit results
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/flora.csv data/flora_meta.json \
                  data/flora_with_omc.csv data/flora_with_omc_meta.json \
                  data/impact_factor.html data/impact_factor_meta.json \
                  data/impact_factor_figs/ \
                  cache/openalex_venues.json || true
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Mean Citedness refresh: $(date -u +'%Y-%m-%d %H:%M UTC')"
            git push
          fi
